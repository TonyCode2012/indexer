schema: 'https://api.lens.dev'
documents: './src/lens/graphql/*.graphql'
generates:
  ./src/lens/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
      - fragment-matcher
    config:
      fetcher: fetch
      dedupeFragments: true
